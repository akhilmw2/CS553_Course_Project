// Cinnamon / Akka Insights requires Akka commercial credentials and a tokenized
// resolver. Keep this disabled by default so a clean checkout can run sbt test.
// Enable it only after configuring credentials from https://akka.io/token.
//
// addSbtPlugin("com.lightbend.cinnamon" % "sbt-cinnamon" % "2.21.4")
