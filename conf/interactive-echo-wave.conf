sim {
  seed = 5532026

  run {
    durationSeconds = 60
  }

  messages {
    types = [ "CONTROL", "PING", "GOSSIP", "WORK", "ACK" ]
  }

  algorithms {
    enabled = [ "echo", "wave" ]
    initiatorNode = 0
  }

  edgeLabeling {
    default = [ "CONTROL", "PING", "WORK" ]
    overrides = []
  }

  traffic {
    defaultPdf = [
      { msg = "PING", p = 0.50 },
      { msg = "WORK", p = 0.50 }
    ]

    perNodePdf = []
  }

  initiators {
    timers = []

    inputs = [
      { node = 0 }
    ]
  }
}
